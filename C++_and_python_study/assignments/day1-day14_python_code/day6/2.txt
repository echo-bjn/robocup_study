1.代码1：
a为一个元组，利用'*'来对元组进行解包

2.代码2：
a为一个元组，利用多个变量x，y，z来对元组进行解包

3.代码3：
a为一个元组，用多个变量对元组进行解包时，利用'_'可以占位，如果同时用多个'_',print(_)打印的是'_'保存的最后一个数，如某个变量加上了'*'表示该变量是一个列表被解包之后被进行赋值，所以y或者_会输出一个对应的列表[2,3]

4.代码4：
a，b构成一个元组，然后将该元组再赋值b，a，使得值发生交换
